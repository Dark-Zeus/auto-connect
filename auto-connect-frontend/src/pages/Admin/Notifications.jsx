import React from 'react';
import NotificationTable from '@components/AdminComponents/NotificationTable';
const Notifications = () => {
  return (
      <NotificationTable />
  );
};

export default Notifications;
